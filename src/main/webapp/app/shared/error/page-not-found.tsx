import React from 'react';

import { Alert } from 'reactstrap';

const PageNotFound = () => {
  return (
    <div>
      <Alert color="danger">Trang không tồn tại.</Alert>
    </div>
  );
};

export default PageNotFound;
